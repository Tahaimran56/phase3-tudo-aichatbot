# Frontend API URL - uses /api routes which go to FastAPI backend
NEXT_PUBLIC_API_URL=/api

# Note: No BACKEND_API_URL needed when deploying to Vercel
# The /api routes automatically go to the FastAPI backend via vercel.json rewrites
